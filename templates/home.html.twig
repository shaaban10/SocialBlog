{% extends 'base.html.twig' %}
{% import 'Macros/post.twig' as post %}

{% block title %}
    {{ parent() }}  - Home
{% endblock %}


{% block body %}
    <body class="hold-transition sidebar-mini">
    <div class="wrapper">
        {% include 'navbar.html.twig' %}
        {% include 'sideMenu.html.twig' %}
        <!-- Content Wrapper. Contains page content -->
        <div class="content-wrapper">
            <!-- Content Header (Page header) -->
            <section class="content-header">
                <div class="container-fluid">
                    <div class="row mb-2">
                        <div class="col-12 text-center">
                            <h2 class="mt-4 mb-4"> Master Blog All Members Posts.</h2>
                            {% if is_granted("ROLE_ADMIN")== true %}
                            
<h5 class="mt-4 mb-4" style="font-family: 'Roboto', sans-serif; font-size: 24px; font-weight: 700; color: #333; text-transform: uppercase;">
    <span style="color: #007bff;">{{ account.getFullName() }}</span> 🦸🦸
    <span style="font-size: 18px; color: #28a745;">You signed as an admin</span>
</h5>                    
{% endif %}
   </div>
                    </div>
                </div><!-- /.container-fluid -->
            </section>
            <!-- Main content -->
            <section class="content">
                <div class="container-fluid">
                    {% include 'new_post.html.twig' %}
                    {% for post in posts %}
                    <div class="row posts">
                        {{ post.post(post,account) }}
                    </div>
                    {% endfor %}
                </div>
            </section>
        </div>

        {% include 'footer.html.twig' %}
     </div>

    </body>
{% endblock %}

